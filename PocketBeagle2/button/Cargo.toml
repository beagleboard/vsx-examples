[package]
name = "button"
description = "Simple example to detect button presses"
version = "0.1.0"
license.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
linux-embedded-hal = { version = "0.4.0", default-features = false, features = ["gpio_cdev"] }
